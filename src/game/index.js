import start from './start.js';
import onChannelMessage from './onChannelMessage.js';

const exports = {
    start,
    onChannelMessage
};

export {
    start,
    onChannelMessage,

    exports as default
}